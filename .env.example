# Bu dosyayı .env olarak kopyalayıp yerel geliştirme için kullanın.
# Bu değişkenler, servisi 'docker-compose' olmadan çalıştırırken gereklidir.

# Worker'ın bağlanacağı PostgreSQL veritabanının tam adresi.
DATABASE_URL=postgresql+psycopg2://azura:supersecurepass123@localhost:5432/azuraforge

# Worker'ın bağlanacağı Redis sunucusunun adresi.
REDIS_URL=redis://localhost:6379/0

# Worker'ın raporları ve modelleri kaydedeceği dizin.
REPORTS_DIR=../platform/reports

# Worker'ın veri setlerini önbelleğe alacağı dizin.
CACHE_DIR=../platform/.cache

# Worker'ın kullanacağı hesaplama cihazı. 'cpu' veya 'gpu' olabilir.
AZURAFORGE_DEVICE=cpu